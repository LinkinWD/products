<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <H1><%=category%> tuotteet</H1>
    <ul>
        <% for(let product of products) { %>
            <li> <a href="/products/<%=product._id%> "><%= product.name  %> </a> </li>
            
        <% } %>
    </ul>
    <a href="/product/new">Uusi tuote</a>
    <% if(category !== 'All')  {%> 
        <a href="/products">Kaikki tuotteet</a>
    <% } %> 
    
</body>
</html>